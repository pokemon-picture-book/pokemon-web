export declare type OPokemonItem = {
    id: number;
    imageColor: string;
    name: string;
    gameImagePath: string;
    types: { code: string }[];
};

export declare type OPokemonState = {
    page: number;
};

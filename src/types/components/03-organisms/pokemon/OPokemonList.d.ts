export declare type OPokemonItem = {
    id: number;
    imageColor: string;
    name: string;
    mainImagePath: string;
    types: { code: string }[];
};

export declare type OPokemonState = {
    isLoading: boolean;
};

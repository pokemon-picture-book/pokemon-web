export declare type StatusChartDataset = {
    label: string;
    /* [hp, attack, special_attack, speed, special_defense, defense] */
    data: number[];
    /* rgba color (ex: 255.0.0) */
    rgbaColor: `${number}, ${number}, ${number}`;
};
